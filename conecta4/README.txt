gcc -o conecta4 main.c ---------------------- gcc(compilar en c) -o(une todos los archivos .o) conecta4(nombre de tu ejecutable) main.c(codigo fuente)

makefile = archivo que une todo el codigo en uno para al hacer el "make <nombre de tu juego(conecta4)>" se cree el ejecutable del programa

Las librerias del main.c se ponen en comillas porque son subprogramas que nosotros hemos creado y no son librerias de linux.


